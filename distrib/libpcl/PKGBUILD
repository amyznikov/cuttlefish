
pkgname=('libpcl')
revhash=$(cd ${SOURCE_DIR} && git rev-parse --short HEAD)
pkgver=$(sed -n 's/^PACKAGE_VERSION\s*=\s*//p' ${SOURCE_DIR}/Makefile | tr - _)~${revhash}
pkgrel=1
arch=('i686' 'x86' 'x86_64')

package() {
	make -C ${SOURCE_DIR} V=1 install DESTDIR=${pkgdir}
}
